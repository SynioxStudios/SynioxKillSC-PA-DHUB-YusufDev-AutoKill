-- [[ Syniox Paid Kill - Karma Görünür Versiyon ]] --

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RS = game:GetService("RunService")
local Lighting = game:GetService("Lighting")

_G.KillActive = true
_G.HopInterval = 60
local SecretAreaPos = CFrame.new(1947, 2, 6191)

task.spawn(function()
    -[span_2](start_span)- 1. ADIM: ANINDA TP[span_2](end_span)
    local function instantTP()
        local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        hrp.CFrame = SecretAreaPos
    end
    instantTP()
    LocalPlayer.CharacterAdded:Connect(instantTP)

    -- 2. ADIM: OPTİMİZASYON DÖNGÜSÜ (KARMA KORUMALI)
    while true do
        pcall(function()
            -[span_3](start_span)- GUI Temizliği: Syniox ve Karma yazılarına dokunma[span_3](end_span)
            local playerGui = LocalPlayer:WaitForChild("PlayerGui")
            for _, gui in pairs(playerGui:GetChildren()) do
                if gui:IsA("ScreenGui") then
                    -- Karma yazılarını içeren "MessageGui" veya benzeri panelleri korur
                    if gui.Name ~= "SynioxGui" and not gui.Name:find("Karma") and not gui.Name:find("Message") then
                        gui:Destroy()
                    end
                end
            end

            -[span_4](start_span)- Dünya Efektlerini Sil (BillboardGui hariç çünkü karma yazıları bunla çalışır)[span_4](end_span)
            for _, obj in pairs(workspace:GetDescendants()) do
                if (obj:IsA("ParticleEmitter") or obj:IsA("Trail")) and not obj:FindFirstAncestor("Karma") then
                    -[span_5](start_span)- Karma efekti değilse sil[span_5](end_span)
                    -- obj:Destroy() -- Karma yazılarını görmek için burayı kapattık
                end
            end

            -[span_6](start_span)- Lighting Ayarları (Karma yazılarını okumak için parlaklık 1 yapıldı)[span_6](end_span)
            Lighting.Brightness = 1 
            Lighting.GlobalShadows = false
            
            -[span_7](start_span)- Atmosfer efektlerini sil ama gökyüzünü aşırı karartma[span_7](end_span)
            for _, v in pairs(Lighting:GetChildren()) do
                if v:IsA("Atmosphere") or v:IsA("Bloom") or v:IsA("BlurEffect") then
                    [span_8](start_span)v:Destroy()[span_8](end_span)
                end
            end
        end)
        task.wait(5)
    end
end)

-[span_9](start_span)[span_10](start_span)- GUI VE KATLIAM DÖNGÜSÜ BURADAN AŞAĞIYA AYNI DEVAM EDER[span_9](end_span)[span_10](end_span)

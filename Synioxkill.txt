-- [ MODIFIED SYNIOX KILL PAID ] --
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RS = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")

_G.KillActive = true
_G.HopInterval = 50
local SecretAreaPos = CFrame.new(1947, 2, 6191)

-- [ OPTIMIZER & TP ] --
task.spawn(function()
    local function instantTP()
        [span_4](start_span)local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()[span_4](end_span)
        local hrp = char:WaitForChild("HumanoidRootPart")
        hrp.CFrame = SecretAreaPos
    end
    instantTP()
    LocalPlayer.CharacterAdded:Connect(instantTP)

    while true do
        pcall(function()
            [span_5](start_span)Lighting.Brightness = 0[span_5](end_span)
            [span_6](start_span)Lighting.ClockTime = 0[span_6](end_span)
            for _, v in pairs(Lighting:GetChildren()) do
                [span_7](start_span)if v:IsA("Sky") or v:IsA("Atmosphere") then v:Destroy() end[span_7](end_span)
            end
        end)
        task.wait(5)
    end
end)

-- [ GUI SETTINGS - 120x120 ] --
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "SynioxGui"
[span_8](start_span)local MainFrame = Instance.new("Frame", ScreenGui)[span_8](end_span)
[span_9](start_span)MainFrame.Size = UDim2.new(0, 120, 0, 120)[span_9](end_span)
MainFrame.Position = UDim2.new(0.5, -60, 0.5, -60)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Active, MainFrame.Draggable = true, true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 25)
Title.Text = "🔥 Syniox Kill Paid 🔥"
Title.TextSize = 11 -- Yazı büyütüldü
Title.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold

local FPSLabel = Instance.new("TextLabel", MainFrame)
FPSLabel.Position = UDim2.new(0, 0, 0, 30)
FPSLabel.Size = UDim2.new(1, 0, 0, 25)
[span_10](start_span)FPSLabel.TextSize = 15[span_10](end_span)
FPSLabel.TextColor3 = Color3.fromRGB(0, 255, 127)
FPSLabel.BackgroundTransparency = 1

local KillLabel = Instance.new("TextLabel", MainFrame)
KillLabel.Position = UDim2.new(0, 0, 0, 55)
KillLabel.Size = UDim2.new(1, 0, 0, 25)
[span_11](start_span)KillLabel.TextSize = 15[span_11](end_span)
KillLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
KillLabel.BackgroundTransparency = 1

local TimerLabel = Instance.new("TextLabel", MainFrame)
TimerLabel.Position = UDim2.new(0, 0, 0, 85)
TimerLabel.Size = UDim2.new(1, 0, 0, 25)
[span_12](start_span)TimerLabel.TextSize = 20[span_12](end_span)
TimerLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TimerLabel.BackgroundTransparency = 1

-- [ ULTRA FAST KILL ENGINE ] --
local lastUpdate, frameCount = tick(), 0
RS.RenderStepped:Connect(function()
    frameCount = frameCount + 1
    if tick() - lastUpdate >= 1 then
        [span_13](start_span)FPSLabel.Text = "⚡ FPS: " .. frameCount[span_13](end_span)
        frameCount, lastUpdate = 0, tick()
    end
    local kills = LocalPlayer:FindFirstChild("leaderstats") and LocalPlayer.leaderstats:FindFirstChild("Kills")
    [span_14](start_span)KillLabel.Text = "💀 Kills: " .. (kills and tostring(kills.Value) or "0")[span_14](end_span)
end)

task.spawn(function()
    while _G.KillActive do
        pcall(function()
            [span_15](start_span)local char = LocalPlayer.Character[span_15](end_span)
            local punch = LocalPlayer.Backpack:FindFirstChild("Punch") or (char and char:FindFirstChild("Punch"))
            if punch and char:FindFirstChild("RightHand") then
                punch.Parent = char
                -- Vuruş hızı 50'ye çıkarıldı (Hızlandırma)
                [span_16](start_span)for i = 1, 50 do LocalPlayer.muscleEvent:FireServer("punch", "rightHand") end[span_16](end_span)
                
                for _, target in ipairs(Players:GetPlayers()) do
                    if target ~= LocalPlayer and target.Character then
                        local tRoot = target.Character:FindFirstChild("HumanoidRootPart")
                        [span_17](start_span)if tRoot and target.Character:FindFirstChild("Humanoid").Health > 0 then[span_17](end_span)
                            [span_18](start_span)firetouchinterest(char.RightHand, tRoot, 1)[span_18](end_span)
                            firetouchinterest(char.RightHand, tRoot, 0)
                        end
                    end
                end
            end
        end)
        task.wait() -- Daha sık döngü için RS.Stepped yerine boş wait
    end
end)

-- [ SERVER HOP ] --
task.spawn(function()
    local t = _G.HopInterval
    while t > 0 do
        [span_19](start_span)TimerLabel.Text = "⏳ " .. t .. "s"[span_19](end_span)
        task.wait(1); t = t - 1
    end
    -[span_20](start_span)- Server Hop Fonksiyonu Buraya Gelecek[span_20](end_span)
end)

-- [ REVISED SYNIOX KILL PAID - STABLE & FAST ] --
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RS = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")

_G.KillActive = true
_G.HopInterval = 50
[span_0](start_span)local SecretAreaPos = CFrame.new(1947, 2, 6191)[span_0](end_span)

-- [ YUSUF'S OPTIMIZER - RE-CHECKED ] --
task.spawn(function()
    local function instantTP()
        [span_1](start_span)local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()[span_1](end_span)
        local hrp = char:WaitForChild("HumanoidRootPart")
        hrp.CFrame = SecretAreaPos
    end
    instantTP()
    LocalPlayer.CharacterAdded:Connect(instantTP)

    while true do
        pcall(function()
            -- GUI & World Cleanup
            for _, obj in pairs(workspace:GetDescendants()) do
                [span_2](start_span)if obj:IsA("ParticleEmitter") or obj:IsA("Trail") then obj:Destroy() end[span_2](end_span)
            end
            Lighting.Brightness = 0
            Lighting.ClockTime = 0
            [span_3](start_span)Lighting.GlobalShadows = false[span_3](end_span)
        end)
        task.wait(5)
    end
end)

-- [ GUI - 130x130 SIZE UPDATED ] --
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "SynioxGui"

local MainFrame = Instance.new("Frame", ScreenGui)
[span_4](start_span)MainFrame.Size = UDim2.new(0, 130, 0, 130) -- İsteğin üzerine 130x130 yapıldı[span_4](end_span)
MainFrame.Position = UDim2.new(0.5, -65, 0.5, -65)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
MainFrame.BorderSizePixel = 0
MainFrame.Active, MainFrame.Draggable = true, true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 28)
Title.Text = "🔥 Syniox Kill Paid 🔥"
Title.TextSize = 12 -- Yazı büyütüldü
Title.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 12)

local FPSLabel = Instance.new("TextLabel", MainFrame)
FPSLabel.Position = UDim2.new(0, 0, 0, 35)
FPSLabel.Size = UDim2.new(1, 0, 0, 25)
[span_5](start_span)FPSLabel.TextSize = 16 -- Büyütüldü[span_5](end_span)
FPSLabel.TextColor3 = Color3.fromRGB(0, 255, 127)
FPSLabel.BackgroundTransparency = 1
FPSLabel.Font = Enum.Font.GothamBold

local KillLabel = Instance.new("TextLabel", MainFrame)
KillLabel.Position = UDim2.new(0, 0, 0, 65)
KillLabel.Size = UDim2.new(1, 0, 0, 25)
[span_6](start_span)KillLabel.TextSize = 16 -- Büyütüldü[span_6](end_span)
KillLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
KillLabel.BackgroundTransparency = 1
KillLabel.Font = Enum.Font.GothamBold

local TimerLabel = Instance.new("TextLabel", MainFrame)
TimerLabel.Position = UDim2.new(0, 0, 0, 95)
TimerLabel.Size = UDim2.new(1, 0, 0, 30)
[span_7](start_span)TimerLabel.TextSize = 22 -- Büyütüldü[span_7](end_span)
TimerLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TimerLabel.BackgroundTransparency = 1
TimerLabel.Font = Enum.Font.GothamBold

-- [ ULTRA KILL ENGINE - OPTIMIZED ] --
local lastUpdate, frameCount = tick(), 0
RS.RenderStepped:Connect(function()
    frameCount = frameCount + 1
    if tick() - lastUpdate >= 1 then
        FPSLabel.Text = "⚡ FPS: " .. frameCount
        frameCount, lastUpdate = 0, tick()
    end
    local kills = LocalPlayer:FindFirstChild("leaderstats") and LocalPlayer.leaderstats:FindFirstChild("Kills")
    KillLabel.Text = "💀 Kills: " .. (kills and tostring(kills.Value) or "0")
end)

task.spawn(function()
    while _G.KillActive do
        pcall(function()
            local char = LocalPlayer.Character
            local punch = LocalPlayer.Backpack:FindFirstChild("Punch") or (char and char:FindFirstChild("Punch"))
            if punch and char:FindFirstChild("RightHand") then
                punch.Parent = char
                -[span_8](start_span)- Hız artırıldı: Saniyede 60 vuruş isteği[span_8](end_span)
                for i = 1, 60 do 
                    LocalPlayer.muscleEvent:FireServer("punch", "rightHand") 
                end
                
                for _, target in ipairs(Players:GetPlayers()) do
                    if target ~= LocalPlayer and target.Character then
                        local tRoot = target.Character:FindFirstChild("HumanoidRootPart")
                        local tHum = target.Character:FindFirstChild("Humanoid")
                        [span_9](start_span)if tRoot and tHum and tHum.Health > 0 then[span_9](end_span)
                            [span_10](start_span)firetouchinterest(char.RightHand, tRoot, 1)[span_10](end_span)
                            firetouchinterest(char.RightHand, tRoot, 0)
                        end
                    end
                end
            end
        end)
        task.wait() -- Maksimum hız için kısa bekleme
    end
end)

-- [ FULL SERVER HOP ] --
local function forceHop()
    TimerLabel.Text = "🌀 TP..."
    pcall(function()
        [span_11](start_span)local servers = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Desc&limit=100")).data[span_11](end_span)
        for _, server in pairs(servers) do
            if server.playing < server.maxPlayers and server.id ~= game.JobId then
                [span_12](start_span)TeleportService:TeleportToPlaceInstance(game.PlaceId, server.id, LocalPlayer)[span_12](end_span)
            end
        end
    end)
end

task.spawn(function()
    local t = _G.HopInterval
    while t > 0 do
        TimerLabel.Text = "⏳ " .. t .. "s"
        task.wait(1); t = t - 1
    end
    forceHop()
end)
